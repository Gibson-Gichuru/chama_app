import {
    Box,
    Typography
} from "@mui/material"

function Transactions() {
  return (
    <Box>
        <Typography variant = "body1">Transactions</Typography>
    </Box>
  )
}

export default Transactions